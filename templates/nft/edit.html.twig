{% extends 'base.html.twig' %}

{% block title %}Edit Nft{% endblock %}

{% block body %}
    <h1>Edit Nft</h1>


    {{ form_start(nftEditForm) }}

    {{ form_label(nftEditForm.price) }}
    {{ form_widget(nftEditForm.price) }}
    {{ form_label(nftEditForm.description) }}
    {{ form_widget(nftEditForm.description) }}


    <button type="submit" class="custom-btn-edit">Update</button>

    {{ form_end(nftEditForm) }}


    {{ include('nft/_delete_form.html.twig') }}

    <a href="{{ path('app_nft_index') }}" class="custom-btn-back-to-list">back to list</a>

{% endblock %}
